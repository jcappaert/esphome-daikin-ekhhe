# Configuration

This page will document ESPHome YAML setup, component options, examples, entity groups, production mode, debug mode, and TX send calibration.

Example YAML files will live under [`../examples`](../examples/).
